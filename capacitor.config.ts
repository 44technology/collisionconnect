import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.collisionconnect.app",
  appName: "Collision Collect",
  webDir: "dist",
};

export default config;
